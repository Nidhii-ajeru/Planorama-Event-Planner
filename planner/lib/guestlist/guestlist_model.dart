import '/backend/backend.dart';
import '/flutter_flow/flutter_flow_util.dart';
import 'guestlist_widget.dart' show GuestlistWidget;
import 'package:flutter/material.dart';

class GuestlistModel extends FlutterFlowModel<GuestlistWidget> {
  ///  State fields for stateful widgets in this page.

  // State field(s) for CheckboxListTile widget.
  Map<GuestRecord, bool> checkboxListTileValueMap1 = {};
  List<GuestRecord> get checkboxListTileCheckedItems1 =>
      checkboxListTileValueMap1.entries
          .where((e) => e.value)
          .map((e) => e.key)
          .toList();

  // State field(s) for CheckboxListTile widget.
  Map<GuestRecord, bool> checkboxListTileValueMap2 = {};
  List<GuestRecord> get checkboxListTileCheckedItems2 =>
      checkboxListTileValueMap2.entries
          .where((e) => e.value)
          .map((e) => e.key)
          .toList();

  // State field(s) for CheckboxListTile widget.
  Map<GuestRecord, bool> checkboxListTileValueMap3 = {};
  List<GuestRecord> get checkboxListTileCheckedItems3 =>
      checkboxListTileValueMap3.entries
          .where((e) => e.value)
          .map((e) => e.key)
          .toList();

  // State field(s) for CheckboxListTile widget.
  Map<GuestRecord, bool> checkboxListTileValueMap4 = {};
  List<GuestRecord> get checkboxListTileCheckedItems4 =>
      checkboxListTileValueMap4.entries
          .where((e) => e.value)
          .map((e) => e.key)
          .toList();

  // State field(s) for CheckboxListTile widget.
  Map<GuestRecord, bool> checkboxListTileValueMap5 = {};
  List<GuestRecord> get checkboxListTileCheckedItems5 =>
      checkboxListTileValueMap5.entries
          .where((e) => e.value)
          .map((e) => e.key)
          .toList();

  @override
  void initState(BuildContext context) {}

  @override
  void dispose() {}
}
